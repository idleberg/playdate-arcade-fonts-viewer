name: Create Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  BUILD_OUTPUT: viewer

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - uses: idleberg/create-playdate-release@test
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
